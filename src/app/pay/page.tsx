"use client"
import React from "react";
import styled from 'styled-components';
import TopBar from "@/components/TopBar";

const Pay = () => {
  return (
      <TopBar text="결제하기" />
  );
};

export default Pay;